if flatpack brew 2> /dev/null; then
    flatpack info io.atom.Atom 2>&1 > /dev/null
    alias atom='flatpak run io.atom.Atom'
fi

if [ -d ${LB_SFW_TOOLS_HOME}/atom ]; then
    pathadd ${LB_SFW_TOOLS_HOME}/atom
fi
